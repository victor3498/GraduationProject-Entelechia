5.1用户身份认证与管理模块
5.1.1数据库实现
用户实体类（User）负责存储用户的基本信息，包括用户ID、用户名、密码哈希等字段。通过PostgreSQL数据库将数据持久化存储。
在数据库中，用户信息保存在`users`表中，其中`id`是主键，唯一标识每个用户，`username`存储用户名，`password_hash`存储加密后的密码。通过`user.model.js`实现对数据库的操作，提供了注册、登录、密码修改等功能的接口，操作包括插入、查询和更新用户数据。
5.1.2前端实现
（1）登录功能
登录界面通过`LoginOrRes.tsx`实现，设计简洁的表单，包含用户名和密码输入框。组件负责处理用户登录逻辑，利用`localStorage`存储和读取用户凭据（accessToken和refreshToken）。登录成功后保存token到`localStorage`并跳转到主界面。
（2）注册功能
注册界面与登录界面共用同一个组件，通过tab切换显示。包含用户名、密码和确认密码输入框，实现了密码一致性校验。注册成功后提示用户并自动切换到登录页面。
（3）密码修改功能
在个人用户界面能够修改用户密码。
5.1.3后端实现
本模块的主要类图结构如图5-1所示
...
 
图5-1 用户身份认证与管理模块类图
（1）登录功能
在登录过程中，系统接收用户输入的用户名和密码，调用`authService.login()`方法验证用户身份。系统首先根据用户名查询数据库，如果查询到匹配的用户，则验证密码是否正确。若验证成功，系统生成`accessToken`和`refreshToken`，并将`refreshToken`保存到数据库中。登录成功后返回token给前端。
用户验证的核心流程如下：1. 根据用户名查询用户。2. 使用bcrypt比较密码哈希。3. 生成JWT token（accessToken和refreshToken）。4. 保存refreshToken到数据库。5. 返回token给前端
登录功能的核心代码如下：
const login = async (username, password) => {
  const user = await userModel.findByUsername(username)
  if (!user) {
    throw new Error('User not found')
  }

  const match = await comparePassword(password, user.password_hash)
  if (!match) {
    throw new Error('Invalid password')
  }

  const payload = { id: user.id, username: user.username }
  const accessToken = signAccessToken(payload)
  const refreshToken = signRefreshToken(payload)

  const expiresAt = new Date(Date.now() + 7 * 24 * 60 * 60 * 1000)
  
  await refreshTokenModel.saveRefreshToken(user.id, refreshToken, expiresAt)
  await refreshTokenModel.deleteUserRefreshTokens(user.id)

  return { accessToken, refreshToken }
}
（2）注册功能
在用户注册时，系统会调用`authService.register()`方法。系统首先检查输入的用户名是否已存在。若用户名已存在，则返回错误信息提示用户；若用户名未被占用，系统会对密码进行哈希处理，然后将用户信息插入到数据库中。注册成功后返回用户信息。
（3）密码修改功能
密码修改功能允许用户修改密码。用户需要输入旧密码和新密码，系统首先验证旧密码是否正确。若验证通过，系统会对新密码进行哈希处理，并更新数据库中的密码。同时，系统会删除该用户的所有refreshToken，确保用户在其他设备上的登录失效。
5.1.4接口实现
本模块的主要接口实现如表5-1所示。
表5-1 用户身份认证与管理模块接口表
接口名称	接口说明	请求参数	返回值/回调方法
loginApi	用户登录验证	{ username: string, password: string }	{ accessToken: string, refreshToken: string }
registerApi	用户注册	{ username: string, password: string }	{ id: number, username: string }

changePasswordApi	修改密码	{ oldPassword: string, newPassword: string }	成功/失败提示
logoutApi	用户退出登录	{ refreshToken: string }	成功/失败提示
refreshApi	刷新accessToken	{ refreshToken: string }	{ accessToken: string }

