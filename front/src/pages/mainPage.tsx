// import { useNavigate } from "react-router-dom"

export default function mainPage() {
  // const navigate = useNavigate()

  return (
    <div>
      这是页面 B
      {/* <button onClick={() => navigate("/C/1")}>
        去 C 页面
      </button> */}
    </div>
  )
}