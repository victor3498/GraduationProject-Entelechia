文档分享与基础权限管理 实现方案
0. 设计决策（基于回答）
访问者：匿名/游客可直接访问，登录页与主页都能跳转分享码输入页
链接策略：每文档最多两条（view、edit 各一条），由 (document_id, permission) 唯一索引保证
生命周期：仅支持撤销，不设过期；撤销采用软删除 (is_active=false) 便于审计
不修改现有四张表，新增一张 document_shares 表
1. 整体数据流
主页/文档项→分享按钮
POST /api/doc/:id/share
通过分享码访问
侧栏入口
提交分享码
GET /api/share/:code (公开)
PUT /api/share/:code/content (edit only)
返回按钮
文档所有者
ShareDialog生成/复制/撤销
share.controller(authMiddleware)
LoginOrRes / MobileLoginOrRes
/share分享码输入页
MainPage / MobileMainPage
/share/:codeSharedDocPage
share.controller(无鉴权)
上一页 location.state.from
2. 数据库（新增 1 张表）
迁移 SQL（建议放 server/migrations/2026xx_create_document_shares.sql，或在 README 写明手动执行）：

CREATE TABLE document_shares (
  id           SERIAL PRIMARY KEY,
  document_id  INTEGER NOT NULL REFERENCES documents(id) ON DELETE CASCADE,
  share_code   VARCHAR(16) NOT NULL UNIQUE,
  permission   VARCHAR(8)  NOT NULL CHECK (permission IN ('view','edit')),
  is_active    BOOLEAN     NOT NULL DEFAULT true,
  created_at   TIMESTAMP   NOT NULL DEFAULT CURRENT_TIMESTAMP,
  revoked_at   TIMESTAMP
);
CREATE UNIQUE INDEX idx_active_share_per_perm
  ON document_shares (document_id, permission)
  WHERE is_active = true;
CREATE INDEX idx_share_code ON document_shares(share_code) WHERE is_active = true;
要点：

部分唯一索引保证「每文档每权限只能有一条生效记录」，但允许保留撤销历史
share_code 用 nanoid 或 crypto.randomBytes 生成 10–12 位 [A-Za-z0-9]，对外暴露的 URL 短且不可猜
ON DELETE CASCADE：原文档被删时，所有分享记录自动清理
3. 后端实现
3.1 新增文件
server/models/share.model.js：CRUD（紧扣 server/models/document.model.js 的写法风格）
createShare({ documentId, shareCode, permission })
getActiveShareByDocAndPerm({ documentId, permission })
getActiveByCode(shareCode) → 联表查 documents 取 id, title, content, updated_at 与 permission
revokeShareByCode({ shareCode, userId }) → 联 documents 校验 user_id = userId
listActiveSharesByDoc({ documentId, userId })
updateContentByShareCode({ shareCode, content }) → 仅当 permission='edit' AND is_active=true 时联表更新 documents.content，并刷新 updated_at
server/services/share.service.js：调用 model + 生成 share_code（用 Node 内置 crypto.randomBytes(8).toString('base64url').slice(0,12)，避免新依赖）
server/controllers/share.controller.js：返回结构沿用 server/utils/response.js 的 success
server/routes/share.routes.js：公开路由，不挂 authMiddleware
3.2 路由注册
修改 server/routes/index.js：

import shareRoutes from './share.routes.js'
router.use('/share', shareRoutes)            // 公开访问
修改 server/routes/document.routes.js 在已有 authMiddleware 之下新增：

router.post('/:id/share', controller.createShare)        // 生成/返回分享码
router.get('/:id/shares', controller.listShares)         // 列出当前文档生效的两条
router.delete('/:id/share/:code', controller.revokeShare)// 撤销
3.3 接口清单
路径	方法	鉴权	入参	返回
/api/doc/:id/share
POST
登录+所有者
{ permission: 'view'|'edit' }
{ shareCode, permission, createdAt }
/api/doc/:id/shares
GET
登录+所有者
—
[{ shareCode, permission, createdAt }, ...]
/api/doc/:id/share/:code
DELETE
登录+所有者
—
{ revoked: true }
/api/share/:code
GET
公开
—
{ docId, title, content, updatedAt, permission }
/api/share/:code/content
PUT
公开（edit 才允许）
{ content }
{ updatedAt }
权限判断：

POST/DELETE 通过 WHERE documents.user_id = $userId 实现「只有所有者能管理」，无需在 controller 单独做 SQL 查询
PUT 内容保存先 getActiveByCode(code)，若 permission !== 'edit' 抛 403
任何无效/已撤销的 code 一律返回 404 { message: 'Share link not found or revoked' }，避免泄漏存在性
3.4 安全/最小修改原则
不修改 documents 表结构；匿名编辑通过 share_code 关联到原文档，作者归属保持不变
不动 authMiddleware；公开路由完全脱离 JWT
复用已有 errorMiddleware，业务异常 throw new Error('xxx') 即可
4. 前端实现
4.1 类型与 API 层（新增）
front/src/types/share.ts
export type SharePermission = 'view' | 'edit'
export interface ShareInfo { shareCode: string; permission: SharePermission; createdAt: string }
export interface SharedDocDetail { docId: number; title: string; content: JSONContent; updatedAt: string; permission: SharePermission }
front/src/api/share.ts：使用现有 front/src/lib/http.ts；getSharedDocApi 与 saveSharedDocApi 走公开路由（仍可携带 token，后端忽略即可）
4.2 路由改造（公开路由，不套 ProtectedRoute）
修改 front/src/router/routes.tsx：

const SharePage = lazy(() => import('../pages/SharePage'))
const SharedDocPage = lazy(() => import('../pages/SharedDocPage'))
// ...
{ path: '/share', element: <SharePage /> },
{ path: '/share/:code', element: <SharedDocPage /> },
修改 front/src/router/mobile-routes.tsx：同样新增 /mobile/share 与 /mobile/share/:code。

4.3 新增页面
front/src/pages/SharePage.tsx：单输入框 + 提交按钮，提交后 navigate('/share/' + code, { state: { from: location.state?.from ?? '/login' } })
front/src/pages/SharedDocPage.tsx：
useParams 取 code → getSharedDocApi(code) 拉 {title, content, permission}
渲染 <SimpleEditor readOnly={permission==='view'} content={content} docTitle={title} onCustomSave={...}/>（见 4.5）
顶部展示「返回」按钮：navigate(location.state?.from ?? -1)；view 模式 banner 提示「只读分享」
移动端对应：front/src/pages/mobile/MobileSharePage.tsx、front/src/pages/mobile/MobileSharedDocPage.tsx，移动端编辑器复用 front/src/components/tiptap-templates/simple/simple-editor-mobile.tsx
4.4 ShareDialog 与入口集成
新增组件 front/src/components/component/document/ShareDialog.tsx：

接收 documentId，挂载时 getSharesApi(documentId)
列出两行（view / edit），每行显示 URL（window.location.origin + '/share/' + code）+ 复制 + 撤销，无则显示「生成」按钮
撤销后立即刷新本地状态
集成入口：

位置	改动
front/src/pages/LoginOrRes.tsx
在 tab 切换下方新增「通过分享码访问」文字按钮 → navigate('/share', { state: { from: '/login' } })
front/src/pages/mobile/MobileLoginOrRes.tsx
同上，目标 /mobile/share
front/src/pages/mainPage.tsx
左侧导航新增一个分享按钮（在 SetIcon 上方）；并把当前 selectedDoc 传入 ShareDialog
front/src/pages/mobile/MobileMainPage.tsx
侧栏新增「分享码访问」入口；编辑器顶栏新增「分享」按钮触发 Dialog
front/src/components/component/document/DocumentItem.tsx
行内操作菜单追加「分享」一项，调用回调由父级（mainPage）打开 ShareDialog
新增图标：front/src/components/component/svg/share.tsx（与已存在的 front/src/components/component/svg/set.tsx 风格一致）。

4.5 SimpleEditor 改造（最小侵入）
修改 front/src/components/tiptap-templates/simple/simple-editor.tsx：

SimpleEditorProps 增加：
readOnly?: boolean
onCustomSave?: (content: JSONContent) => Promise<void>   // 分享场景使用
hideToolbar?: boolean
useEditor({ ..., editable: !readOnly })
handleSave 优先调用 onCustomSave（用于分享 PUT）；否则保留原有 saveDocApi(docId, ...) 逻辑
view 权限时 hideToolbar 隐藏整个 <Toolbar> 或仅保留返回按钮
移动端 simple-editor-mobile.tsx 做同等扩展
这样 SharedDocPage 直接复用编辑器，无需新建编辑器组件，避免代码重复。

5. 关键边界与异常处理
失效链接：GET /api/share/:code 返回 404 → 前端展示「该分享链接已撤销或不存在」+ 返回按钮
view 模式下编辑：前端在 editable=false 已禁用；后端 PUT 仍校验权限，二级防护
同一权限重复生成：直接返回当前生效记录的 share_code（POST 是幂等操作）
分享码冲突：模型层在唯一约束失败时重试一次（生成新随机码）
未登录用户的 http 拦截：front/src/lib/http.ts 现在遇到 Invalid or expired token 会强制跳 /login；分享路由是公开的，建议在 http.ts 中对 /share/* 路径跳过自动重定向，否则游客访问会被踢回登录页