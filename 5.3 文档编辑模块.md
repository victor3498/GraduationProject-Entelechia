## 5.3 文档编辑模块

### 5.3.1 编辑器核心实现

文档编辑模块基于Tiptap富文本编辑器实现，提供丰富的文本编辑功能。编辑器支持多种文本格式，包括标题、段落、列表、引用、代码块等。通过扩展机制，实现了图片上传、高亮标记、上下标等高级功能。

编辑器内容以JSON格式存储，确保了数据的结构化和可扩展性。文档内容通过`simple-editor.tsx`组件渲染，提供流畅的编辑体验。

### 5.3.2 前端实现

#### （1）编辑器初始化
编辑器通过`SimpleEditor`组件实现，初始化时加载文档内容。组件接收`content`属性作为初始内容，并通过`onContentChange`回调函数实时传递内容变化。

#### （2）工具栏实现
工具栏提供了丰富的编辑功能按钮，包括撤销/重做、标题选择、列表类型、文本样式（粗体、斜体、删除线等）、颜色高亮、链接插入、文本对齐等。工具栏支持响应式设计，在移动端自动适配显示。

#### （3）文档内容编辑
用户在编辑器中输入和修改内容时，系统实时监听内容变化，并通过`onContentChange`回调通知父组件。编辑器支持多种文本格式，包括：
- 标题（H1-H4）
- 有序列表和无序列表
- 任务列表
- 引用块
- 代码块
- 粗体、斜体、删除线、下划线、行内代码
- 颜色高亮
- 上标和下标
- 文本对齐（左对齐、居中、右对齐、两端对齐）

#### （4）文档标题编辑
文档标题通过编辑器顶部的输入框实现，用户可以直接修改标题，系统实时调用`onTitleChange`回调通知父组件。

#### （5）文档保存功能
保存功能通过工具栏中的保存按钮触发，点击后调用`saveDocApi`接口将文档内容保存到服务器。保存成功后显示成功提示，失败则显示错误信息。

### 5.3.3 后端实现

本模块的后端主要类图结构如图5-3所示。

```mermaid
classDiagram
    class DocumentController {
        -documentService: DocumentService
        +getDocumentDetail(req, res, next)
        +saveDocumentContent(req, res, next)
    }
    
    class DocumentService {
        -documentModel: DocumentModel
        +getDocumentDetail(userId, documentId, withContent)
        +saveDocumentContent(userId, documentId, content)
    }
    
    class DocumentModel {
        -db: PostgreSQL
        +getDocumentById({userId, documentId, withContent})
        +updateDocumentContent({userId, documentId, content})
    }
    
    DocumentController --> DocumentService : uses
    DocumentService --> DocumentModel : uses
```

图5-3 文档编辑模块后端类图

#### （1）获取文档详情
在获取文档详情时，系统根据文档ID和用户ID查询数据库，返回文档的完整信息，包括内容字段。支持通过`withContent`参数控制是否返回内容，以优化性能。

#### （2）保存文档内容
在保存文档内容时，系统接收文档ID和新内容，更新数据库中的文档记录，并更新`updated_at`字段。保存成功后返回更新后的文档信息。

保存文档内容功能的核心代码如下：

```javascript
async function updateDocumentContent({ userId, documentId, content }) {
  const sql = `
    UPDATE documents
    SET content = $1,
        updated_at = CURRENT_TIMESTAMP
    WHERE id = $2 AND user_id = $3
    RETURNING id, updated_at
  `
  const { rows } = await db.query(sql, [content, documentId, userId])
  return rows[0]
}
```

### 5.3.4 接口实现

本模块功能的主要接口实现如下表所示：

| 接口名称 | 接口说明 | 请求参数 | 返回值/回调方法 |
| :--- | :--- | :--- | :--- |
| `getDocDetailApi` | 获取文档详情 | `documentId: number, withContent?: boolean` | `{ id: number, title: string, content: JSONContent, is_starred: boolean, created_at: string, updated_at: string }` |
| `saveDocApi` | 保存文档内容 | `{ content: JSONContent }` | `{ id: number, updated_at: string }` |