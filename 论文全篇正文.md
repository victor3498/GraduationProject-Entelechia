## 不包含图表等非文字信息。

> 说明：本次修饰对原文做语言书面化与语病修正，并补全/规范标点。**所有改动处的字词或标点均以加粗形式标注**，便于核对；段落格式经过整理（每段之间空行分隔），原文内容不增不减。少量明显笔误（例如"提提供""起步与G端"等）已在原位修正，并以同样方式加粗标注。

---

## 1.1 引言

随着个人计算机的普及与互联网技术的发展，文档编辑方式逐步由传统本地软件向在线化、协同化方向演进。从早期的 Bravo 到以 Google Docs 为代表的在线文档工具，文档处理模式不断革新，并推动了基于云端的协同办公应用**的**发展。在此背景下，在线文档系统已成为现代信息处理与协同工作的重要工具。基于上述发展趋势，本文围绕在线文档编辑系统的设计与实现展开研究。

## 1.2 研究背景以及意义

个人计算机大致从 20 世纪 80 年代起开始普及，世界上第一个"所见即所得"文字编辑器 Bravo **则于 1974 年由施乐 PARC 开发完成**[1]。自此本地文字处理软件以及文档编辑软件一直作为计算机软件生态圈**的重要组成部分**不断发展。**早在** 2006 年，Google **便**推出了第一版在线文档程序 Google Documents，**即** Google Docs 的雏形，并在 2012 年正式改名为 Google Docs，文档编辑应用**的**发展进入新阶段，不再仅仅局限于本地购买**形式**的软件，**而是**出现了基于 SaaS 概念**的**新生态。此后**，**微软 Office 365、石墨文档等产品不断出现，诸如文档处理、项目管理、知识库建设等功能也得到实现。这些在线文档应用能够很好地满足员工协同办公的需要，无需下载，支持多人在线编辑**、**实时保存与同步查看**。**

在线文档应用**的**需求与发展在之后的 2020 年全球新冠疫情时期**得到了进一步推动**，基于客观条件的限制，**其**开始承担网络基础设施的功能，以线上协同的方式替代线下聚集，成为基层社区、组织信息采集与治理的重要工具[2]。2021 年河南省汛情中群策群力的"救命文档"则作为网络时代下衍生的新型"不见面办公"的典型案例**，进一步受到**广大传播学**、**社会学领域**学者的关注与研究**，国外也有研究充分证明了 Google Docs 对教学领域的助力[3]，**由此可见**，在线文档应用在现代办公环境之外同样能发挥重要作用。

目前市场上各个厂商旗下的在线文档应用服务**已较为完善**，无论是企业还是个人**，**在工作学习上的**各种**需求基本都能覆盖。本课题开发的在线文档编辑系统旨在从个人能力出发，**致力于**构建出一个个性化、轻量化的线上文档编辑系统。用户可以直接通过网页访问的方式在双端（电脑端**、**移动端）进行个人文档的创建**、**编辑**与**管理。

## 1.3 国内外研究现状

### 1.3.1 国内研究现状

近年来，随着云计算技术、移动互联网以及企业数字化转型的不断推进，在线文档应用逐渐成为支撑协同办公与知识管理的重要基础工具，国内也**涌现出**一批具有代表性的功能齐备的在线文档产品，如石墨文档**、**腾讯文档**、**金山文档等，下面**主要围绕这三个产品**进行说明。首先是**于** 2015 年由武汉初心科技有限公司推出的石墨文档，目前侧重于发展 B 端大中型企业用户，首推在线协同文档私有化部署方案，根据企业业务场景以及管理流程，实现可定制化的协同文档平台**。**

接着是**同于** 2018 年推出的腾讯文档与金山文档。腾讯文档借助微信 **C** 端流量优势，作为后发者快速实现了用户**规模**的迅速增长[4]，同时因其具有腾讯生态支持的优势，能够**便捷地进行**编辑与分享。腾讯文档**还联合**腾讯云搭建了低代码开发平台，实现对企业的助力。

金山文档则**起步于** G 端，受信创产业推动，在 G 端构建出了优势。其旨在打造多元的协同办公产品矩阵，目前**已有**"金山协作"**、**"轻维表"和"金山知识库"三款产品。同时其研发**围绕**"云和数字化"方向**进行**长期储备，并**与**华为云等**厂商展开**战略合作[5]。

### 1.3.2 国外研究现状

国外**早在** 20 世纪 80 年代就已经出现早期的协同写作系统，此时主要用于探索多人写作的可行性与基本机制，如文本共享、简单版本管理和有限的协作控制等。这一阶段为**后续**在线文档**在**协同功能方面**的发展**打下了坚实的理论基础。

真正意义上的现代在线文档——现代协同编辑文档应用**被**广泛认为始于 2006 年前后，基于 Google Docs**，**人们真正完成了第一次现代意义上的协同写作。Google Docs 通过浏览器提供多人实时编辑能力，结合云端存储与共享机制，使协同写作从实验性系统转变为可规模化应用的成熟产品。而随着云计算与网络通信技术的发展，国外**的**在线文档应用**领域**逐渐形成以 Google Docs 和 Microsoft Word Online（Microsoft 365）为代表的成熟产品体系。这类工具不仅支持联合文本创作，还涵盖文本修订、共享评论与版本回溯等功能。**在此基础上，**这些应用**进一步**融合了变更追踪**等**传统单人写作软件的高级功能。同时，协同编辑已不再局限于单一文档层面，而是嵌入到更大规模的协作平台之中，如 Microsoft Teams 和 Google Workspace，通过与文件管理、即时通信、项目管理等功能的整合，构建起面向组织级协作的综合解决方案。

国内外在线文档应用普遍涉及两大技术点。其一是协同算法 Operational Transformation（OT），这是实现其协同操作的关键算法**。**其基本思想是：用户产生的操作在本地立即执行并应用到数据副本上，**若**直接应用到其他协作者的数据副本上**则**会产生冲突，**此时需**将用户的操作进行转换**以**解决并发冲突，最终实现所有用户的数据一致[6]。协同编辑作为重要功能**，**提供了快捷高效的合作模式，保障了团队成员**之间能**畅通地进行文档协作编辑，在高校及企业团队内部的信息交流、协同办公、wiki 写作等方面都有很重要的意义[7]。

其二是当下广泛使用的 WebSocket 实时通信机制。WebSocket 通过在客户端与服务器之间建立并保持一个持久的、全双工的双向通信通道，使网页能够在无需频繁重新建立连接或整体刷新页面的情况下，实现数据的实时更新[8]。相较于传统的 HTTP 轮询或长轮询方式，WebSocket 能够显著降低通信延迟与网络开销，使编辑操作、光标位置以及用户在线状态等信息**能够以**低延迟、高频率的方式进行同步。在在线文档应用中，用户的编辑行为通常会被封装为操作事件，通过 WebSocket 通道即时发送至服务器并广播给其他在线协作者，从而实现接近实时的协同编辑体验。该通信机制为协同算法的高效运行提供了基础保障，也是现代在线文档系统实现流畅交互和良好用户体验的重要技术支撑**。**

## 1.4 研究内容

本课题的主要工作是开发一款轻量化 SaaS 在线文档编辑应用，用户能够直接通过双端访问应用进行文档的创建、编辑以及管理。主要工作内容包括以下几个方面：

（1）需求分析与系统功能设计：参照当下主流在线文档应用的功能设计与使用模式，并结合问卷调查等方式，分析目标用户在文档编辑、存储与管理过程中的实际需求。在此基础上，明确系统的核心功能需求，包括用户身份认证、个人文档管理、在线编辑与文档分享等功能，确定系统的功能边界与设计目标，为后续系统设计与实现提供依据。

（2）系统总体设计：系统总体设计采用前后端分离的分层架构，包括前端展示层、后端业务逻辑层与数据存储层。前端负责用户交互与文档编辑界面的展示，后端负责用户认证、业务逻辑处理与数据管理，数据层用于存储用户信息与文档内容。系统设计注重模块划分与接口规范，通过用户模块、文档管理模块、文档编辑模块等功能模块的划分，提高系统的可扩展性与可维护性。

（3）系统开发与实现：在系统设计的基础上，完成各功能模块的具体实现，包括用户注册与登录、基于身份认证的访问控制、个人文档的创建、编辑、删除与查询，以及在线文档编辑功能的实现。文档编辑模块基于富文本编辑器实现常用文本编辑功能，并支持文档内容的保存与分享。

（4）系统测试与优化：对系统进行功能测试与性能测试，验证用户认证、文档管理和编辑功能的正确性与稳定性。针对测试过程中发现的问题进行优化，重点关注系统在多用户访问情况下的响应速度与稳定性，确保系统在不同使用场景下能够正常运行，满足毕业设计对系统完整性和可靠性的要求。

## 1.5 论文结构

本论文整体共有六章**。**第一章是绪论，**交**代研究背景、意义以及国内外发展现状；第二章是相关技术基础，讲述系统涉及的 JS 后端开发**、**JS 前端开发**、**部署上线服务器以及富文本编辑器；第三章是需求分析，基于实际用例分析明确系统各方面需求；第四章是系统设计，阐述系统架构设计**、**数据库设计以及功能模块设计；第五章是系统实现，详细阐述各个功能模块的具体实现；第六章是**系统测试，对系统进行功能与非功能层面的验证**；最后是结论，对本篇论文以及项目进行总结和展望**。**

> 备注：原文"第六章是；"内容缺失，依据全文（如结论中"系统测试阶段"等表述）补出"系统测试"一项以使句子完整，**如不希望补出，可将上文加粗补出的内容删除**。

---

## 2.1 JS 后端开发

JavaScript 固然不常用来进行后端开发，但随着 Node.js 技术的不断成熟，JS 已经广泛应用于后端开发**：例如，**Netflix 采用 Node.js 构建其部分服务端系统，以提升页面加载性能与接口响应效率；PayPal 通过引入 Node.js 重构其后端系统，进而提升开发效率**等等**。**这些应用实例表明，**对于一些前后端统一技术栈的项目，或者本身需要构建的服务**为**轻量级微服务时**，**使用 JS 进行后端开发是合适的选择。

当下 JS 后端开发依赖于 Node.js。Node.js 基于 V8 引擎构建，使得 JS 本身能脱离浏览器**而**独立在服务端执行。Node.js 采用事件驱动和非阻塞 I/O 的设计模型，应用运行在单一进程中，通过异步机制处理网络请求、数据库访问和文件系统操作，避免了传统多线程模型中频繁创建和切换线程所带来的性能开销[9]。同时**，**在实际的 **Web** 应用开发中，通常还会结合**诸如** Express.js 这样的轻量级框架，这类框架提供了路由管理、中间件机制等核心功能，有力地提高了系统的可拓展性和可维护性。

系统架构设计方面，**JS 后端开发依旧沿用传统后端开发的设计习惯**。整体架构**呈现为** "model-server-controller" **形式**，将数据模型、业务逻辑与接口控制进行分离，并以 RESTful API 的形式对外提供接口，通过统一的 HTTP 方法对资源进行操作，并通过 JSON 格式与前端进行数据交互。

数据存储方面，**JS 后端常用的数据库**包括关系**型**数据库（**如** MySQL）与非关系**型**数据库（如 MongoDB）**，本项目开发采用的数据库为** PostgreSQL。PostgreSQL 具备良好的稳定性与事务支持能力**[10]，**自 2001 年以来始终符合 ACID 事务特性，能够在复杂数据负载场景下保证数据的一致性与完整性，从而满足系统在数据安全性与可靠性方面的需求。**此外，**PostgreSQL 提供的 JSONB 类型适合对富文本内容进行存储，通过 JSONB **可**实现对富文本结构化数据的高效存储与查询，为后续文档编辑、保存等功能提供数据支持。

## 2.2 JS 前端开发

作为为前端开发而生的语言，使用 JavaScript 进行前端开发已经是相当成熟的技术。

JS 是一种基于 ECMAScript 标准**、**最初运行于浏览器环境**、**用于实现网页的动态交互功能的标准脚本语言。发展到现在，该语言已经能够支持模块化、面向对象及异步编程。运行机制方面，JavaScript 采用单线程执行模型，并通过事件循环（Event Loop）实现异步任务调度。开发者可利用 Promise、async/await 等机制处理异步操作，从而提升程序在高并发场景下的执行效率。

工程实践层面**，**JS 前端开发有着相当完善的生态体系，囊括了项目构建（**如** Webpack**、**Vite）**、**依赖管理（**如** npm**、**pnpm**、**yarn）**、**常用框架（**如** Vue.js**、**React）**、**状态管理（**如** Zustand**、**pinia**、**Redux）**、**代码规范等**多个方面**。其中具有代表性的 React 和 Vue.js 前端框架**尤为重要**，两个框架采用虚拟 DOM 技术，显著地优化了大中型前端**应用**的渲染性能，并且引入了关键的组件化开发思想，鼓励将用户界面拆分**为**多个独立**可**复用的模块，有利于增强系统**的**可维护性。此外，现代前端框架提供了丰富的开发抽象与工具机制。例如 Vue.js 的组合式 API（Composition API）以及 React 的 Hooks 机制，通过对组件逻辑的结构化组织与复用能力的增强，使前端开发过程更加直观、高效。

## 2.3 上线部署服务器

项目完成后的部署是系统落地的重要收尾环节。对于独立开发者的小型项目而言，通常**采用**轻量化、低成本的部署方案，将应用运行于云服务器环境中，以实现对外访问与持续运行。常见方式是基于 Linux 服务器进行部署，结合远程连接工具完成环境配置与项目上传，并通过命令行完成服务启动与管理。对于本项目而言，后端服务基于 Node.js 运行环境部署，并借助进程管理工具（如 PM2）实现服务的守护运行与异常重启；前端项目则通过构建工具打包生成静态资源，并结合 Nginx 进行静态资源托管与反向代理配置，从而实现前后端统一访问入口。此外，域名解析与端口映射等配置也构成了系统对外服务的基础。

## 2.4 富文本编辑器

作为现代 Web 应用中不可或缺的交互组件，富文本编辑器的核心技术原理围绕文档模型、状态管理和视图渲染展开。主流实现通常采用分层架构，将内容抽象为独立的文档模型（如 ProseMirror 的节点树结构），通过不可变数据结构管理编辑器状态，并利用虚拟 DOM 或类似技术实现高效的视图更新。编辑器内部通过事务（Transaction）机制处理用户操作，将复杂编辑行为分解为最小操作单元（Step），确保状态变更的可追踪性和可撤销性。

工程实践层面，富文本编辑器生态体系已相当成熟，涵盖了核心库、扩展插件、集成方案等多个维度。核心库方面，ProseMirror 以其强大的架构设计和可扩展性成为行业标杆，提供了完整的文档模型、状态管理和视图渲染解决方案；Quill.js 则以简洁**的** API 和良好的开箱即用体验**而**受到青睐；TinyMCE 作为老牌编辑器，在企业级应用中**被**广泛使用。扩展插件生态丰富，支持表格、媒体嵌入、协作编辑等高级功能。集成方案上，现代前端框架如 React、Vue.js 均提供了相应的组件封装，简化了编辑器的集成过程。笔者**所采用的是基于 ProseMirror 进行包装与简化的** TipTap：TipTap 是一个基于 ProseMirror 构建的无头（Headless）富文本编辑框架，通过扩展机制和命令式 API 提供高度灵活的编辑能力，能够根据应用需求定制编辑功能[11]。ProseMirror 的设计理念尤为值得关注**：**它采用 Schema 机制定义文档结构，确保内容的一致性和有效性；通过 Position 和 Mapping 系统处理文档位置定位与更新；利用插件系统实现功能扩展，支持从简单的文本格式化到复杂的协作编辑等多种场景。**而** TipTap 则**作为基于 ProseMirror 的封装库**，进一步简化了使用门槛，提供了更友好的 API 和开箱即用的功能，**非常**适合初次接触的开发者。

## 2.5 本章小结

本章讲述了项目开发过程中涉及的四个主要方面——JS 后端开发、JS 前端开发、服务器部署上线以及富文本编辑器。通过深入理解相关技术的原理**、**功能以及运用场景，**组合并明确了**技术方案，为整个项目的实现奠定了坚实基础。接下来的章节将进一步展开描述系统需求分析与具体模块的设计实现。

---

## 第三章 需求分析

## 3.1 系统功能需求分析

### 3.1.1 需求概述

> 备注：原标题后括注的"[4.1]"疑为编辑过程中遗留的笔记标记，**为保持原文不增删，未删除处理**；如需删除可径行去掉。

本项目旨在开发一款轻量化 SaaS 在线文档编辑应用，专注于为用户提供双端**的**消息及文本**的**记录、编辑与保存等核心功能。系统具备专业的富文本编辑能力，支持多格式排版、代码块等丰富功能，满足用户多样化的编辑需求。技术实现上，系统**采用** TipTap 作为编辑器核心，结合 JavaScript 前后端技术栈，在确保功能完备的同时，为用户带来轻量便捷的使用体验。

系统从功能层面划分为**四个**主要功能，分别为用户身份认证与管理功能**、**文档管理功能**、**文档编辑功能**与文档分享功能**。功能结构图如图 3-1 所示。

> 备注：原文为"三个主要功能"，但其后实际描述了四大模块（含"文档分享功能"），存在前后不一致；为与下文及第五章描述统一，已修订为"四个"。**如不希望补出，可将该处加粗内容改回"三个"，但仍建议同步修正。**

总用例图如图 3-2 所示，该用例图展示了用户在本系统中的主要操作路径以及模块间逻辑结构。

**（1）**用户身份认证与管理功能

用户身份认证与管理模块主要负责用户的注册、验证以及数据**的保存与维护**。主要功能为注册、登录以及个人信息修改。系统基于用户名与密码进行身份校验。注册时需要设置用户名以及密码，同时会对用户名进行校验从而保证唯一性；并且会对密码字段进行长度与字符组合校验，确保基本安全性。注册成功后用户可以进行登录操作进入主页，进一步使用文档操作功能。系统还支持用户在个人中心界面修改密码与用户名，**其验证流程与注册时一致**。

本模块的关键用例如表 3-1 到表 3-3 所示**。**

**（2）**文档管理功能

文档管理模块是整个系统应用的核心部分**，**负责文档的查看、筛选、创建、删除等**功能**。同时用户也**通过**该模块提供的功能服务进入编辑器页面与用户页面**，**使用其他功能。本模块的关键用例如表 3-4 至表 3-8 **所示。**

**（3）**文档编辑功能

文档编辑模块承担着整个系统最主要的功能。通过富文本编辑器，用户能够自行修改**与**调整自己账户下的在线文档并**进行实时**持久化。移动端的文本编辑页面因受页面尺寸限制**，**主要提供**若干**基本功能，而 PC 端则提供了更全面的多样化编辑修改功能，如代码块**、**超链接等。本模块的关键用例如表 3-9 至**表** 3-10 所示。

**（4）**文档分享功能

文档分享模块是系统实现文档协作与共享的核心模块**，**负责文档分享码的生成、分享权限的管理、分享记录的查看与撤销等功能。同时支持访客通过分享码公开访问文档，实现多人编辑文档。本模块的关键用例如表 3-11 至表 3-15 **所示。**

## 3.2 系统非功能需求分析

### 3.2.1 性能需求

（1）文档加载：文档内容加载时间不超过 1 秒，大型文档（10000 字以上）加载时间不超过 3 秒**。**

（2）编辑器响应：用户输入时编辑器响应延迟不超过 100 ms**。**

（3）保存响应：文档保存操作完成时间不超过 500 ms**。**

（4）列表渲染：文档列表页面加载时间不超过 800 ms，支持 1000 条以上文档的快速滚动**。**

### 3.2.2 安全性需求

（1）用户认证：采用 JWT 令牌认证机制，令牌有效期 2 小时，支持自动刷新**。**

（2）密码安全：采用 BCrypt 算法加密存储，密码强度要求至少 8 位，包含数字和字母**。**

## 3.3 本章小结

本章主要对系统进行需求分析，将系统功能划分为四大模块进行分析，并辅以用例规约表进行详细说明。同时还对系统进行非功能需求分析，包括性能需求与安全性需求，从而确保使用体验与数据安全**。**

---

## 4.1 系统架构设计

### 4.1.1 系统架构概述

系统的整体架构如图 4-1 所示，**基于前后端分离的层次化架构思想，划分为** View 层**、**Presenter 层**、**Service 层与 Model 层，各层功能明确**、**职责清晰，相互协作的同时逻辑独立**、**互不干扰，在实现核心功能的同时保证了系统的拓展性、可维护性与高效性能。

### 4.1.2 各层功能描述

（1）View 层：负责界面展示与用户交互，捕获用户操作并将事件传递给 Presenter。通过 Pages 与 Components 组织各业务界面，包含主页面、登录注册页、文档编辑器、移动端页面等，仅承担输入输出职责，不处理业务逻辑。

（2）Presenter 层：承担业务逻辑处理，接收 View 层事件，调用 Model 层获取或更新数据，并将结果反馈给 View 层。包含用户认证、文档管理等独立模块，保证系统逻辑的清晰与高内聚。

（3）Service 层：处理后端业务逻辑，接收 Presenter 层请求，调用数据层执行数据库操作，完成身份验证、文档 CRUD、偏好存储等功能，并返回处理结果。

（4）Model 层：负责数据持久化存储，包含数据库存储用户信息、文档数据、刷新令牌以及 LocalStorage 存储前端临时数据，为 Presenter 层提供数据读写支持。

## 4.2 数据库设计

### 4.2.1 数据库概述

为了实现用户管理**、**在线文档创建**、**在线文档编辑等功能，系统设计了本地 PostgreSQL 数据库，用以持久化各类业务数据。数据库的 E-R 模型如图 4-2 所示**。**

系统采用关系数据库模型，共设计五张数据表，包含用户信息**、**文档信息等数据。各表通过主键与外键关联，构成严谨的数据结构。

## 4.3 界面设计

### 4.3.1 登录界面

（1）PC 端界面组成与交互逻辑

登录界面**如图 4-3 所示，采用左右布局**，左侧为轮播图区域**，**展示 4 张风景图片，底部配有圆形指示器；右侧是能够 Tab 切换的登录注册复合组件，包含 Tab 切换控件、标题副标题、用户名密码输入框、登录/注册按钮和错误提示区。Tab 切换控件下蓝色字体部分为进入输入文档分享码界面**的**入口**。**

交互逻辑上，点击 Tab 切换登录表单；密码输入时实时验证强度；点击登录按钮**，**成功则跳转主页，失败则显示错误；**轮播图**每 6 秒自动切换，点击指示器可手动切换。点击 Tab 切换控件下蓝色字体则**跳转至文档分享码输入界面。**

（2）移动端界面组成与交互逻辑

界面中部白色圆角容器**中**放置登录注册复合组件，如图 4-4**所示**，包含 Tab 切换控件、标题副标题、用户名密码输入框、登录/注册按钮和错误提示区。交互逻辑上与 PC 端一致。

### 4.3.2 注册界面

（1）PC 端界面组成与交互逻辑

与登录界面为统一界面，如图 4-5**所示**，通过复合组件 Tab 标签切换显示注册表单；表单包含用户名输入框、密码输入框、确认密码输入框；注册按钮在验证通过后可点击。

交互逻辑上，密码输入框通过回调实时验证密码强度；确认密码与密码实时比对，不一致时显示错误；表单验证未通过时按钮禁用；注册成功后自动切换到登录界面 Tab。

（2）移动端界面组成与交互逻辑

与 PC 端一致，**仅在**组件尺寸方面作移动端适配**，如图 4-6 所示。**

### 4.3.3 主页

（1）PC 端界面组成与交互逻辑

如图 4-7**所示**，页面**采用**三栏式布局，左侧导航栏包含用户头像、所有文档按钮、标星文档按钮、新建文档按钮、用户设置按钮、分享码输入页面按钮和退出登录按钮；中间文档列表包含搜索工具栏、文档列表项；右侧编辑器区域占据剩余空间，包含文档标题栏、编辑器内容区域，标题栏显示标题、标星标记和更新时间。

交互逻辑上，点击文档列表项打开文档；点击星标切换标星状态；点击删除按钮**，**确认后删除文档；点击分享按钮进入分享页面；点击新建按钮创建新文档；点击导航按钮切换"所有文档"/"标星文档"视图；搜索框输入实时过滤文档列表；文档内容变化时实时检测，点击保存按钮保存文档，标题可编辑更新。

（2）移动端界面组成与交互逻辑

如图 4-8**所示**，顶部导航栏包含左侧选项按钮、右侧新建按钮；主内容区包含搜索工具栏和可滚动文档列表**。**侧边栏为抽屉式设计，固定定位在左侧，包含用户头像和名称区域、导航按钮组、底部退出登录按钮，默认隐藏，点击选项按钮显示。

交互逻辑上，点击选项按钮显示/隐藏侧边栏；点击侧边栏遮罩层关闭侧边栏；点击导航按钮切换"所有文档"/"标星文档"视图并关闭侧边栏；点击文档列表项打开文档到编辑器视图；点击新建按钮创建新文档；点击退出登录按钮**，**确认后跳转登录页；文档列表项支持标星、删除和分享操作。

### 4.3.4 编辑界面

（1）PC 端界面组成与交互逻辑

如图 4-9**所示**，**界面位于主页最右侧。工具栏**包含撤销/重做按钮、标题下拉菜单（H1-H4）、列表下拉菜单、引用按钮、代码块按钮、**加粗、斜体、删除线、行内代码、下划线、高亮、链接按钮**、上下标按钮、对齐方式按钮和保存按钮；编辑器内容区域支持标题、**段落**、有序列表、无序列表、任务列表、引用块、代码块、文字高亮**等格式**。

> 备注：原文此处为"标题、分享、有序列表"，依据上下文（编辑器格式而非分享操作）疑为笔误，已改为"段落"，**如确为分享请改回**。

交互逻辑上，在编辑区域点击即可对文字内容进行编辑，**选中部分文字后**可点击工具栏按钮进行效果操作，点击保存按钮保存文档**；**点击分享按钮进入分享页面。

（2）移动端界面组成与交互逻辑

如图 4-10**所示**，编辑器内容区域占据全屏空间，支持标题、列表、任务列表、引用块、代码块、文字高亮等内容格式。工具栏位于页面下方，含文字格式化按钮组（加粗、斜体、删除线、行内代码、下划线）、高亮按钮、保存按钮和返回按钮。交互逻辑上与 PC 端界面基本一致，点击返回按钮返回主页。

### 4.3.5 个人信息界面

（1）PC 端界面组成与交互逻辑

如图 4-11**所示**，**界面**左上角**为带箭头图标的**返回按钮，点击返回主页；页面标题**"个人资料"以**大字居中显示；表单区域**包含**旧密码、新密码、确认密码三个输入框；提交按钮全宽显示；错误提示为红色背景，成功提示为绿色背景。

交互逻辑上，用户输入旧密码和新密码；验证新密码与确认密码**的**一致性；点击提交按钮触发密码**修改**；修改成功后跳转到登录页；修改失败**则**显示错误提示。

（2）移动端界面组成与交互逻辑

如图 4-12**所示**，界面与交互逻辑**基本与** PC 端一致。

### 4.3.6 文档分享界面

#### 4.3.6.1 分享管理界面

（1）PC 端界面组成与交互逻辑

如图 4-12**所示**，分享管理弹窗以模态对话框形式呈现，点击主页文档的分享按钮后在屏幕中央弹出。弹窗顶部显示标题以及当前文档名称，右上角有关闭按钮；内容区纵向排列"仅查看"和"可编辑"两个分享链接区块，每个区块包含权限标签徽章、权限说明文字和操作按钮——若尚未生成分享链接，**则**显示生成链接按钮；若已生成，则显示完整分享 URL 的只读输入框、复制按钮与撤销按钮；底部有关闭按钮；操作失败时在内容区底部显示红色错误提示。

交互逻辑上，点击生成链接按钮后**，**系统生成对应权限的分享码，区块随即更新为链接展示状态；点击复制按钮**，**将完整链接写入剪贴板，按钮文字短暂变为"已复制"以给出反馈；点击撤销按钮**，**弹出确认提示，确认后撤销该条分享链接并清空对应区块**，**回到未生成状态；点击关闭按钮**或**标题栏按钮**均**可关闭弹窗。

（2）移动端界面组成与交互逻辑

如图 4-13**所示**，移动端复用同一组件，弹窗宽度自适应屏幕宽度，界面结构与内容区**划分**与 PC 端保持一致，仅触控目标尺寸适当放大以适配触屏操作，交互逻辑与 PC 端界面基本一致。

#### 4.3.6.2 分享码输入界面

（1）PC 端界面组成与交互逻辑

如图 4-14**所示**，分享码输入页面为独立的公开访问页面，无需登录即可进入。顶部导航栏显示应用名称，主体区域居中展示一张白色卡片，卡片内依次包含页面标题"通过分享码访问文档"、说明文字、分享码/链接文本输入框**及**返回按钮**。**

交互逻辑上，用户在输入框中输入分享码或粘贴完整分享链接，系统实时解析并提取有效分享码；点击进入文档按钮或按下回车键后跳转到共享文档页面；点击返回按钮回到来源页面。

（2）移动端界面组成与交互逻辑

如图 4-15**所示**，顶部导航栏左侧为返回箭头按钮，中央显示标题"通过分享码访问"；主体区域自顶向下依次排列说明文字、分享码/链接输入框、返回按钮和"进入文档"按钮。交互逻辑与 PC 端界面基本一致，点击返回箭头按钮回到来源页面。

#### 4.3.6.3 共享文档访问界面

（1）PC 端界面组成与交互逻辑

如图 4-16 与**图** 4-17 所示，共享文档页面分为顶部信息栏与编辑器内容区两部分。顶部信息栏固定在页面顶端，左侧包含返回按钮、文档标题和权限标签徽章——只读分享显示灰色"只读分享"标签，可编辑分享显示蓝色"可编辑分享"标签；右侧展示文档最后更新时间；编辑器内容区复用桌面端编辑器组件**：**只读分享下工具栏不可操作，仅供阅览；可编辑分享下工具栏全部启用，支持对文档内容进行修改与保存。加载文档过程中页面居中展示旋转动效；若分享码无效或已被撤销，则显示"该分享链接已撤销或不存在"**提示**和返回按钮。

交互逻辑上，页面加载时根据 URL 路径中的分享码请求文档详情，无需登录即可访问；只读权限下用户只能浏览文档内容，无法编辑；可编辑权限下用户可在编辑区域直接修改内容，点击保存按钮触发分享专属保存接口，顶部信息栏实时反馈保存状态；点击返回按钮返回来源页面。

（2）移动端界面组成与交互逻辑

如图 4-18 与**图** 4-19 所示，移动端共享文档页面顶部导航栏左侧为返回箭头按钮，中央显示文档标题，右侧显示简化权限标签；导航栏下方为占满剩余屏幕高度的内容区，复用移动端编辑器组件**：**可编辑分享下工具栏位于页面底部，只读分享下工具栏不显示，按钮触控尺寸适配移动端。交互逻辑与 PC 端界面基本一致，点击返回按钮返回来源页面。

## 4.4 系统功能模块设计

### 4.4.1 用户身份认证与管理模块

该模块主要功能是管理用户信息**，包括**注册、登录、修改信息等。主要流程图如图 4-13 所示**。**系统采用用户名与密码的组合进行身份认证。基于此模块，用户可以注册新账户**、**登录已经创建的账户并修改账户的基本信息（用户名以及密码）**。**

### 4.4.2 文档管理模块

文档管理模块负责对用户文档进行整体管理和操作，主要功能包括：进入模块后自动加载用户的所有文档列表，支持按"全部"和"标星"两种视图筛选文档，提供关键词搜索功能**以**帮助快速定位目标文档；同时支持新建文档（通过输入标题创建）、删除文档（需确认）、标星/取消标星文档以及重命名文档等操作，形成完整的文档生命周期管理能力**。**流程图见图 4-14**。**

### 4.4.3 文档编辑模块

文档编辑模块专注于文档内容的编辑和处理，主要功能包括：选择文档后**，**从服务器加载文档详情和内容，在基于 Tiptap 构建的富文本编辑器中进行内容编辑，支持标题、列表、代码块、引用等多种格式；编辑过程中可随时保存文档到服务器，也支持修改文档标题，确保文档内容和元信息的实时同步更新。本模块主要流程图如图 4-15 所示**。**

### 4.4.4 文档分享模块

文档分享模块负责文档分享链接的生成、管理和公开访问功能，主要功能包括：进入文档编辑页面后可选择生成分享链接，支持"只读"和"可编辑"两种权限类型；同一文档对每种权限最多生成一条生效分享记录，再次生成时自动返回已存在的分享码；支持查看当前生效的分享列表，提供撤销分享链接功能（采用软删除机制保留历史记录）；其他用户可通过分享链接直接访问或编辑文档，无需登录系统即可进行协作。本模块主要流程图如图 4-16 所示。

## 4.5 本章小结

本章主要介绍了系统的总体设计**：**通过架构图说明系统架构并概述各层功能**；**同时基于 ER 图与数据库表设计讲述了具体的数据存储和管理方式，并且详细介绍各功能界面的组成以及交互逻辑**；**最后结合项目需求**，**以流程图的形式介绍了各主要模块的功能设计。

---

## 5.1 用户身份认证与管理模块

### 5.1.1 数据库实现

用户实体类（User）负责存储用户的基本信息，包括用户 ID、用户名、密码哈希等字段。通过 PostgreSQL 数据库将数据持久化存储。

在数据库中，用户信息保存在 `users` 表中，其中 `id` 是主键，唯一标识每个用户，`username` 存储用户名，`password_hash` 存储加密后的密码。通过 `user.model.js` 实现对数据库的操作，提供了注册、登录、密码修改等功能的接口，操作包括插入、查询和更新用户数据。

### 5.1.2 前端实现

（1）登录功能

登录界面通过 `LoginOrRes.tsx` 实现，**采用**简洁的表单设计，包含用户名和密码输入框。组件负责处理用户登录逻辑，利用 `localStorage` 存储和读取用户凭据（accessToken 和 refreshToken）。登录成功后**，将** token 保存到 `localStorage` 并跳转到主界面。

（2）注册功能

注册界面与登录界面共用同一个组件，通过 tab 切换显示**，**包含用户名、密码和确认密码输入框，实现了密码一致性校验。注册成功后提示用户并自动切换到登录页面。

（3）密码修改功能

在个人用户界面**中**能够修改用户密码。

### 5.1.3 后端实现

本模块的主要类图结构如图 5-1 所示**。**

（1）登录功能

在登录过程中，系统接收用户输入的用户名和密码，调用 `authService.login()` 方法验证用户身份。系统首先根据用户名查询数据库，如果查询到匹配的用户，则验证密码是否正确。若验证成功，系统生成 `accessToken` 和 `refreshToken`，并将 `refreshToken` 保存到数据库中。登录成功后**，**返回 token 给前端。

用户验证的核心流程如下：1. 根据用户名查询用户**；** 2. 使用 bcrypt 比较密码哈希**；** 3. 生成 JWT token（accessToken 和 refreshToken）**；** 4. 保存 refreshToken 到数据库**；** 5. 返回 token 给前端**。**

登录功能的核心代码如下：

（2）注册功能

在用户注册时，系统会调用 `authService.register()` 方法。系统首先检查输入的用户名是否已存在。若用户名已存在，则返回错误信息提示用户；若用户名未被占用，系统会对密码进行哈希处理，然后将用户信息插入到数据库中。注册成功后**，**返回用户信息。

（3）密码修改功能

密码修改功能允许用户修改密码。用户需要输入旧密码和新密码，系统首先验证旧密码是否正确。若验证通过，系统会对新密码进行哈希处理，并更新数据库中的密码。同时，系统会删除该用户的所有 refreshToken，确保用户在其他设备上的登录失效。

## 5.2 文档管理模块

### 5.2.1 数据库实现

文档实体类（Document）负责存储文档的基本信息，包括文档 ID、用户 ID、标题、内容、是否标星、创建时间和更新时间等字段。通过 PostgreSQL 数据库将数据持久化存储，确保了数据存取的高效性和安全性。

在数据库中，文档信息保存在 `documents` 表中，其中 `id` 是主键，唯一标识每个文档**；** `user_id` 关联到用户表**；** `title` 存储文档标题**；** `content` 存储文档内容（JSON 格式）**；** `is_starred` 标识文档是否被标星**；** `created_at` 和 `updated_at` 分别记录文档的创建时间和更新时间。通过 `document.model.js` 实现对数据库的操作，提供了新建、删除、重命名、查询等功能的接口。

### 5.2.2 前端实现

（1）文档列表管理

文档列表通过 `MainPage.tsx` 组件实现，包含左侧导航栏、中间文档列表区域。左侧导航栏提供视图切换功能（全部文档/标星文档）和新建文档按钮。中间区域展示文档列表，支持搜索功能和分页展示。

（2）文档筛选功能

筛选功能通过 `DocumentToolbar` 组件实现，用户输入关键词后，系统调用 `searchDocApi` 接口查询匹配的文档，并实时更新文档列表。

（3）标星管理功能

标星功能允许用户标记重要文档。点击文档列表中的标星图标，系统调用 `starDocApi` 接口更新文档的标星状态，并实时更新界面显示。

### 5.2.3 后端实现

本模块的后端主要类图结构如图 5-2 所示。

（1）获取文档列表

在获取文档列表时，系统根据用户 ID 查询数据库，返回该用户的所有文档列表，按更新时间降序排列。返回的文档信息不包含内容字段，以提高查询性能。

（2）新建文档

在新建文档时，系统接收用户输入的标题，创建一个新的文档记录。文档内容默认为一个包含标题的空文档结构。创建成功后**，**返回文档的基本信息。

（3）删除文档

在删除文档时，系统根据文档 ID 和用户 ID 执行删除操作。删除前需要验证文档是否存在且属于当前用户。删除成功后**，**返回成功提示**。**

（4）重命名文档

在重命名文档时，系统接收新的标题，更新文档记录，并更新 `updated_at` 字段。重命名成功后**，**返回更新后的文档信息。

（5）标星/取消标星

在标星/取消标星时，系统根据文档 ID 和用户 ID 更新文档的 `is_starred` 字段，并更新 `updated_at` 字段。操作成功后**，**返回更新后的标星状态。

（6）搜索文档

搜索文档功能支持按关键词模糊搜索和按标星状态过滤。系统根据用户输入的条件构建 SQL 查询，返回匹配的文档列表。搜索文档功能的核心代码如下：

## 5.3 文档编辑模块

### 5.3.1 编辑器核心实现

文档编辑模块基于 Tiptap 富文本编辑器实现，提供丰富的文本编辑功能。编辑器支持多种文本格式，包括标题、段落、列表、引用、代码块等。通过扩展机制，实现了图片上传、高亮标记、上下标等高级功能。

编辑器内容以 JSON 格式存储，确保了数据的结构化和可扩展性。文档内容通过 `simple-editor.tsx` 组件渲染，提供流畅的编辑体验。

### 5.3.2 前端实现

（1）编辑器初始化

编辑器通过 `SimpleEditor` 组件实现，初始化时加载文档内容。组件接收 `content` 属性作为初始内容，并通过 `onContentChange` 回调函数实时传递内容变化。

（2）工具栏实现

工具栏提供了丰富的编辑功能按钮，包括撤销/重做、标题选择、列表类型、文本样式（粗体、斜体、删除线等）、颜色高亮、链接插入、文本对齐等。工具栏支持响应式设计，在移动端自动适配显示。

（3）文档内容编辑

用户在编辑器中输入和修改内容时，系统实时监听内容变化，并通过 `onContentChange` 回调通知父组件。编辑器支持多种文本格式，包括**：**标题（H1-H4）、有序列表和无序列表、任务列表、引用、代码块、粗体、斜体、删除线等**。**

（4）文档保存功能

保存功能通过工具栏中的保存按钮触发，点击后调用 `saveDocApi` 接口将文档内容保存到服务器。保存成功后显示成功提示，失败则显示错误信息。

### 5.3.3 后端实现

本模块的后端主要类图结构如图 5-3 所示。

（1）获取文档详情

在获取文档详情时，系统根据文档 ID 和用户 ID 查询数据库，返回文档的完整信息，包括内容字段。支持通过 `withContent` 参数控制是否返回内容，以优化性能。

（2）保存文档内容

在保存文档内容时，系统接收文档 ID 和新内容，更新数据库中的文档记录，并更新 `updated_at` 字段。保存成功后**，**返回更新后的文档信息。保存文档内容功能的核心代码如下：

## 5.4 文档分享模块

### 5.4.1 数据库实现

分享实体类（Share）负责存储文档分享的基本信息，包括分享 ID、文档 ID、分享码、权限类型、是否生效等字段。通过 PostgreSQL 数据库将数据持久化存储，确保了分享链接的可靠性和可追溯性。

在数据库中，分享信息保存在 `document_shares` 表中，其中 `id` 是主键，唯一标识每条分享记录**；** `document_id` 关联到文档表**；** `share_code` 存储系统生成的分享码**；** `permission` 标识权限类型（view 或 edit）**；** `is_active` 标识分享是否生效**；** `created_at` 和 `revoked_at` 分别记录分享的创建时间和撤销时间。通过 `share.model.js` 实现对数据库的操作，提供了分享创建、查询、撤销等功能的接口。

### 5.4.2 前端实现

（1）分享管理功能

分享管理功能通过 `ShareDialog.tsx` 组件实现，当用户点击文档的分享按钮时弹出该对话框。对话框列出当前文档的 view 和 edit 两条分享链接，每条链接显示权限类型、说明和操作按钮。不存在分享链接时显示"生成链接"按钮；存在时显示完整的分享 URL、复制按钮和撤销按钮。用户点击复制按钮后，系统将分享链接复制到剪贴板，并显示"已复制"提示。

（2）分享码输入功能

分享码输入功能通过 `SharePage.tsx` 实现，该页面作为公开访问入口，无需登录即可访问。用户在该页面输入分享码或直接粘贴完整的分享链接，系统自动解析并提取分享码。输入完成后**，**点击"进入文档"按钮**，即可**跳转到分享文档页面。

（3）共享文档访问功能

共享文档访问功能通过 `SharedDocPage.tsx` 实现，用户通过分享链接访问共享文档时加载该页面。系统根据分享码调用 `getSharedDocApi` 接口获取文档详情，无需登录即可访问。根据分享权限类型，编辑器表现为只读或可编辑状态。可编辑的分享链接支持直接修改文档内容并保存，系统调用 `saveSharedDocApi` 接口完成保存操作。

### 5.4.3 后端实现

本模块的后端主要类图结构如图 5-4 所示。

（1）生成/复用分享码

在生成或复用分享码时，系统调用 `shareService.createOrGetShare()` 方法。系统首先校验权限类型，仅允许 view 或 edit 两种权限。然后验证文档归属，确保只有文档所有者才能创建分享链接。验证通过后，系统查询是否存在同权限的生效分享记录，若存在则直接返回现有记录，**以**实现幂等性。若不存在，则生成新的 12 位 base64url 格式分享码并插入数据库。若遇到分享码冲突（概率极低），系统最多重试 3 次。生成/复用分享码功能的核心代码如下**：**

（2）列出分享记录

在列出分享记录时，系统调用 `shareService.listShares()` 方法。系统首先验证文档归属，然后查询该文档当前所有生效的分享记录。每个文档最多存在两条生效分享记录（view 和 edit 各一条），按权限类型升序返回。

（3）撤销分享链接

在撤销分享链接时，系统调用 `shareService.revokeShare()` 方法。系统根据分享码和文档 ID 执行软删除操作，将 `is_active` 字段设为 false，并记录 `revoked_at` 时间戳。撤销操作仅文档所有者可执行，撤销后他人无法再通过该链接访问文档。

（4）获取分享文档

在获取分享文档时，系统调用 `shareService.getSharedDoc()` 方法。这是一个公开接口，**访客**无需登录即可访问。系统通过分享码查询生效的分享记录及其关联的文档信息，返回文档标题、内容、权限类型等详情。若分享码不存在或已撤销，**则**统一返回 404 错误。

（5）保存分享文档内容

在保存分享文档内容时，系统调用 `shareService.saveSharedDocContent()` 方法。这也是一个公开接口，但仅当分享权限为 edit 时**才**允许保存操作。系统首先验证分享码的有效性和权限类型，若权限不足则返回 403 错误。验证通过后，更新文档内容并记录更新时间。

## 5.5 本章小结

本章讲述了系统四大模块的具体实现。用户身份认证与管理模块、文档管理模块以及文档分享模块**均**分为**了**数据库实现、前端实现、后端实现**、接口实现**四个方面进行讲解；文档编辑模块则将数据库实现部分替换为编辑器核心实现。整体上采用图表和文字结合的形式，并在部分关键功能说明中提供了核心代码。

---

## 结论

### 1. 论文工作总结

本论文旨在**完成**在线文档编辑器的研究与开发。全文围绕需求分析**、**系统设计、系统实现与系统测试展开，详细地讲述相关技术与实现细节。

需求分析阶段，基于当前主流在线文档编辑产品分析得出用户身份认证与管理、文档管理、文档编辑以及文档分享四大核心模块。针对系统整体功能架构，采用了前后端分离的技术架构与分层设计思想，前端基于 React 构建组件化应用，后端采用 Express.js 提供 RESTful API 服务，确保系统具有良好的模块化特性和扩展性。同时充分考虑了移动端适配需求，设计了桌面端与移动端两套交互界面，满足不同场景下的编辑需求。

系统设计阶段，详细完成了系统架构和数据库以及接口设计。架构层面采用四层分层设计，包括视图层（View）负责用户界面展示、业务逻辑层（Presenter）处理前端交互逻辑、服务层（Service）封装后端业务处理、数据层（Model）负责数据持久化。数据库采用 PostgreSQL 存储用户信息、文档数据和刷新令牌等核心数据，设计了合理的数据模型与关联关系。接口设计遵循 RESTful 规范，定义了用户认证、文档操作、用户偏好三大类 API 接口，确保了数据的一致性、可靠性及系统响应速度。同时针对各模块完成了界面原型设计和功能流程设计，为后续开发奠定了坚实基础。

系统实现阶段，围绕四大功能模块分别完成了前后端编码实现与接口联调。用户认证模块实现了用户注册、登录、密码修改、Token 刷新与退出功能，采用 JWT 无状态认证机制结合 bcrypt 密码加密，确保用户信息安全。文档管理模块实现了文档的创建、删除、重命名、搜索、标星等功能，完善的权限校验机制确保用户只能操作自有文档。文档编辑模块基于 Tiptap 富文本编辑器，扩展实现了标题、段落、列表、引用、代码块等多种节点类型，支持粗体、斜体、删除线、高亮、链接等文本样式，提供文本对齐**、**撤销重做等编辑功能。文档分享模块实现了文档的阅读与可编辑两种分享模式**，以及**分享码查看与撤销**、**分享文档查阅等功能。

系统测试阶段，依据功能测试与非功能测试相结合的策略，对各功能模块进行了全面的单元测试与集成测试，全面覆盖编辑器各项编辑功能。同时开展了性能测试与安全性测试，验证页面加载速度、编辑器响应性能等**。**测试结果表明，本系统能够稳定实现预期功能，满足用户对在线文档编辑的核心需求。

整体而言，项目通过从需求到设计、从开发到测试的完整研发流程，搭建了一个初步具备实用性的在线富文本编辑平台，积累了宝贵的开发经验。

### 2. 工作展望

目前**已**实现了在线文档编辑器的基础功能开发与验证，但受限于开发者经验有限**、**时间与其他资源有限等原因，应用依旧有较大的提升空间，**后续**在有条件的基础下计划从以下几个方面进行进一步的完善与拓展：

（1）进一步实现协同编辑功能**。**当前系统仅支持单人编辑模式，后续将重点实现多人实时协同编辑功能。通过引入操作日志、冲突检测与合并机制，支持多用户同时在线编辑同一文档，实时同步编辑内容，并显示其他用户的光标位置和编辑状态，提升团队协作效率。

（2）优化后端架构**。**针对当前后端架构进行全面优化，包括引入微服务架构模式，将认证服务、文档服务、用户偏好服务等解耦为独立模块；优化数据库查询性能，增加缓存层（如 Redis）减少数据库压力；实现消息队列机制，处理异步任务如文档版本历史存储、搜索索引更新等；引入容器化部署方案（Docker+Kubernetes），提升系统的可扩展性和运维效率。

（3）用户体验优化与界面优化**。**持续优化用户体验，包括：优化页面加载速度和响应时间；完善移动端适配，提升移动端编辑体验；增加暗色主题切换功能；优化编辑器工具栏布局，支持自定义工具按钮排列；添加文档模板功能，提供常用文档模板（如简历、报告、会议记录等）；实现文档导出功能，支持导出为 Markdown、PDF、Word 等格式；优化错误提示和操作引导，提升用户操作流畅度。
