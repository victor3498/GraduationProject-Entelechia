### Node and Marks
如果你将文档视为一棵树，那么节点就是这棵树中的一种内容。节点的例子有段落、标题或代码块。但节点不一定是块状内容。它们也可以与文本内联呈现，例如@提及。将它们视为可以以不同方式进行样式设置和操作的内容片段。

标记可以应用于节点的特定部分。例如，粗体、斜体或加粗文本。链接也是一种标记。你可以将它们视为一种设计或注释文本的方式。

## 不同之处

节点和标记在某些方面相似，但它们有不同的用例。节点是文档的构建块。它们定义了内容的结构和层次。而标记则用于设置文本样式或添加注释。它们可以应用于节点的任何部分，但不会改变文档的结构。