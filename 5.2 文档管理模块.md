## 5.2 文档管理模块

### 5.2.1 数据库实现

文档实体类（Document）负责存储文档的基本信息，包括文档ID、用户ID、标题、内容、是否标星、创建时间和更新时间等字段。通过PostgreSQL数据库将数据持久化存储，确保了数据存取的高效性和安全性。

在数据库中，文档信息保存在`documents`表中，其中`id`是主键，唯一标识每个文档，`user_id`关联到用户表，`title`存储文档标题，`content`存储文档内容（JSON格式），`is_starred`标识文档是否被标星，`created_at`和`updated_at`分别记录文档的创建时间和更新时间。通过`document.model.js`实现对数据库的操作，提供了新建、删除、重命名、查询等功能的接口。

### 5.2.2 前端实现

#### （1）文档列表管理
文档列表通过`MainPage.tsx`组件实现，包含左侧导航栏、中间文档列表区域和右侧编辑器区域。左侧导航栏提供视图切换功能（全部文档/标星文档）和新建文档按钮。中间区域展示文档列表，支持搜索功能和分页展示。

#### （2）文档搜索功能
搜索功能通过`DocumentToolbar`组件实现，用户输入关键词后，系统调用`searchDocApi`接口查询匹配的文档，并实时更新文档列表。

#### （3）标星管理功能
标星功能允许用户标记重要文档。点击文档列表中的标星图标，系统调用`starDocApi`接口更新文档的标星状态，并实时更新界面显示。

### 5.2.3 后端实现

本模块的后端主要类图结构如图5-2所示。

```mermaid
classDiagram
    class DocumentController {
        -documentService: DocumentService
        +createDocument(req, res, next)
        +deleteDocument(req, res, next)
        +renameDocument(req, res, next)
        +saveDocumentContent(req, res, next)
        +getDocumentList(req, res, next)
        +queryDocuments(req, res, next)
        +getDocumentDetail(req, res, next)
        +toggleStar(req, res, next)
    }
    
    class DocumentService {
        -documentModel: DocumentModel
        +createDocument(userId, title, content)
        +deleteDocument(userId, documentId)
        +renameDocument(userId, documentId, newTitle)
        +saveDocumentContent(userId, documentId, content)
        +getDocumentList(userId)
        +queryDocuments(userId, query)
        +getDocumentDetail(userId, documentId, withContent)
        +toggleStar(userId, documentId, isStarred)
    }
    
    class DocumentModel {
        -db: PostgreSQL
        +createDocument({userId, title, content})
        +deleteDocument({userId, documentId})
        +renameDocument({userId, documentId, title})
        +getDocumentList({userId})
        +getDocumentById({userId, documentId, withContent})
        +toggleStar({userId, documentId, isStarred})
        +updateDocumentContent({userId, documentId, content})
        +queryDocuments({userId, keyword, isStarred})
    }
    
    DocumentController --> DocumentService : uses
    DocumentService --> DocumentModel : uses
```

图5-2 文档管理模块后端类图

#### （1）获取文档列表
在获取文档列表时，系统根据用户ID查询数据库，返回该用户的所有文档列表，按更新时间降序排列。返回的文档信息不包含内容字段，以提高查询性能。

#### （2）新建文档
在新建文档时，系统接收用户输入的标题，创建一个新的文档记录。文档内容默认为一个包含标题的空文档结构。创建成功后返回文档的基本信息。

#### （3）删除文档
在删除文档时，系统根据文档ID和用户ID执行删除操作。删除前需要验证文档是否存在且属于当前用户。删除成功后返回成功提示。

#### （4）重命名文档
在重命名文档时，系统接收新的标题，更新文档记录，并更新`updated_at`字段。重命名成功后返回更新后的文档信息。

#### （5）标星/取消标星
在标星/取消标星时，系统根据文档ID和用户ID更新文档的`is_starred`字段，并更新`updated_at`字段。操作成功后返回更新后的标星状态。

#### （6）搜索文档
搜索文档功能支持按关键词模糊搜索和按标星状态过滤。系统根据用户输入的条件构建SQL查询，返回匹配的文档列表。

搜索文档功能的核心代码如下：

```javascript
async function queryDocuments({ userId, keyword = '', isStarred }) {
  let sql = `
    SELECT id, title, is_starred, created_at, updated_at
    FROM documents
    WHERE user_id = $1
  `
  const params = [userId]
  let index = 2

  if (keyword) {
    sql += ` AND title ILIKE $${index}`
    params.push(`%${keyword}%`)
    index++
  }

  if (typeof isStarred === 'boolean') {
    sql += ` AND is_starred = $${index}`
    params.push(isStarred)
  }

  sql += ` ORDER BY updated_at DESC`

  const { rows } = await db.query(sql, params)
  return rows
}
```

### 5.2.4 接口实现

本模块功能的主要接口实现如下表所示：

| 接口名称 | 接口说明 | 请求参数 | 返回值/回调方法 |
| :--- | :--- | :--- | :--- |
| `getDocListApi` | 获取文档列表 | 无 | `DocumentItem[]` |
| `createDocApi` | 新建文档 | `{ title: string }` | `{ id: number, title: string, is_starred: boolean, created_at: string }` |
| `deleteDocApi` | 删除文档 | `documentId: number` | 成功/失败提示 |
| `renameDocApi` | 重命名文档 | `{ title: string }` | `{ id: number, title: string, updated_at: string }` |
| `starDocApi` | 标星/取消标星 | `{ isStarred: boolean }` | `{ id: number, is_starred: boolean, updated_at: string }` |
| `searchDocApi` | 搜索文档 | `{ keyword?: string, isStarred?: boolean }` | `DocumentItem[]` |