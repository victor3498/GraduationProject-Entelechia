## 5.4 文档分享模块

### 5.4.1 数据库实现

分享实体类（Share）负责存储文档分享的基本信息，包括分享ID、文档ID、分享码、权限类型、是否生效等字段。通过PostgreSQL数据库将数据持久化存储，确保了分享链接的可靠性和可追溯性。

在数据库中，分享信息保存在`document_shares`表中，其中`id`是主键，唯一标识每条分享记录，`document_id`关联到文档表，`share_code`存储系统生成的分享码，`permission`标识权限类型（view或edit），`is_active`标识分享是否生效，`created_at`和`revoked_at`分别记录分享的创建时间和撤销时间。通过`share.model.js`实现对数据库的操作，提供了分享创建、查询、撤销等功能的接口。

### 5.4.2 前端实现

#### （1）分享管理功能

分享管理功能通过`ShareDialog.tsx`组件实现，当用户点击文档的分享按钮时弹出该对话框。对话框列出当前文档的view和edit两条分享链接，每条链接显示权限类型、说明和操作按钮。不存在分享链接时显示「生成链接」按钮；存在时显示完整的分享URL、复制按钮和撤销按钮。用户点击复制按钮后，系统将分享链接复制到剪贴板，并显示「已复制」提示。

#### （2）分享码输入功能

分享码输入功能通过`SharePage.tsx`实现，该页面作为公开访问入口，无需登录即可访问。用户在该页面输入分享码或直接粘贴完整的分享链接，系统自动解析并提取分享码。输入完成后点击「进入文档」按钮，跳转到分享文档页面。

#### （3）共享文档访问功能

共享文档访问功能通过`SharedDocPage.tsx`实现，用户通过分享链接访问共享文档时加载该页面。系统根据分享码调用`getSharedDocApi`接口获取文档详情，无需登录即可访问。根据分享权限类型，编辑器表现为只读或可编辑状态。可编辑的分享链接支持直接修改文档内容并保存，系统调用`saveSharedDocApi`接口完成保存操作。

### 5.4.3 后端实现

本模块的后端主要类图结构如图5-4所示。

```mermaid
classDiagram
    class ShareController {
        -shareService: ShareService
        +createShare(req, res, next)
        +listShares(req, res, next)
        +revokeShare(req, res, next)
        +getSharedDoc(req, res, next)
        +saveSharedDocContent(req, res, next)
    }

    class ShareService {
        -shareModel: ShareModel
        +createOrGetShare({ userId, documentId, permission })
        +listShares({ userId, documentId })
        +revokeShare({ userId, documentId, shareCode })
        +getSharedDoc(shareCode)
        +saveSharedDocContent({ shareCode, content })
    }

    class ShareModel {
        -db: PostgreSQL
        +createShare({ documentId, shareCode, permission })
        +getActiveShareByDocAndPerm({ documentId, permission })
        +listActiveSharesByDoc({ documentId, userId })
        +getActiveByCode(shareCode)
        +revokeShareByCode({ shareCode, documentId, userId })
        +updateContentByShareCode({ shareCode, content })
        +isDocumentOwner({ documentId, userId })
    }

    ShareController --> ShareService : uses
    ShareService --> ShareModel : uses
```

图5-4 文档分享模块后端类图

#### （1）生成/复用分享码

在生成或复用分享码时，系统调用`shareService.createOrGetShare()`方法。系统首先校验权限类型，仅允许view或edit两种权限。然后验证文档归属，确保只有文档所有者才能创建分享链接。验证通过后，系统查询是否存在同权限的生效分享记录，若存在则直接返回现有记录，实现幂等性。若不存在，则生成新的12位base64url格式分享码并插入数据库。若遇到分享码冲突（概率极低），系统最多重试3次。

生成/复用分享码功能的核心代码如下：

```javascript
async function createOrGetShare({ userId, documentId, permission }) {
  if (!ALLOWED_PERMISSIONS.has(permission)) {
    throw makeError('permission must be "view" or "edit"', 400)
  }

  const isOwner = await shareModel.isDocumentOwner({ documentId, userId })
  if (!isOwner) {
    throw makeError('Document not found or no permission', 404)
  }

  const existing = await shareModel.getActiveShareByDocAndPerm({
    documentId,
    permission,
  })
  if (existing) return existing

  let lastErr
  for (let i = 0; i < MAX_RETRY; i++) {
    const shareCode = generateShareCode()
    try {
      return await shareModel.createShare({
        documentId,
        shareCode,
        permission,
      })
    } catch (err) {
      if (err && err.code === '23505') {
        lastErr = err
        const dup = await shareModel.getActiveShareByDocAndPerm({
          documentId,
          permission,
        })
        if (dup) return dup
        continue
      }
      throw err
    }
  }
  throw lastErr || makeError('Failed to create share code', 500)
}
```

#### （2）列出分享记录

在列出分享记录时，系统调用`shareService.listShares()`方法。系统首先验证文档归属，然后查询该文档当前所有生效的分享记录。每个文档最多存在两条生效分享记录（view和edit各一条），按权限类型升序返回。

#### （3）撤销分享链接

在撤销分享链接时，系统调用`shareService.revokeShare()`方法。系统根据分享码和文档ID执行软删除操作，将`is_active`字段设为false，并记录`revoked_at`时间戳。撤销操作仅文档所有者可执行，撤销后他人无法再通过该链接访问文档。

#### （4）获取分享文档

在获取分享文档时，系统调用`shareService.getSharedDoc()`方法。这是一个公开接口，游客无需登录即可访问。系统通过分享码查询生效的分享记录及其关联的文档信息，返回文档标题、内容、权限类型等详情。若分享码不存在或已撤销，统一返回404错误。

#### （5）保存分享文档内容

在保存分享文档内容时，系统调用`shareService.saveSharedDocContent()`方法。这也是一个公开接口，但仅当分享权限为edit时允许保存操作。系统首先验证分享码的有效性和权限类型，若权限不足则返回403错误。验证通过后，更新文档内容并记录更新时间。

### 5.4.4 接口实现

本模块功能的主要接口实现如下表所示：

| 接口名称 | 接口说明 | 请求参数 | 返回值/回调方法 |
| :--- | :--- | :--- | :--- |
| `createShareApi` | 生成/复用分享码 | `documentId: number, permission: 'view'\|'edit'` | `{ shareCode: string, permission: string, createdAt: string }` |
| `listSharesApi` | 列出分享记录 | `documentId: number` | `ShareInfo[]` |
| `revokeShareApi` | 撤销分享链接 | `documentId: number, shareCode: string` | `{ revoked: boolean }` |
| `getSharedDocApi` | 获取分享文档 | `shareCode: string` | `{ docId: number, title: string, content: JSONContent, permission: string, ... }` |
| `saveSharedDocApi` | 保存分享文档 | `shareCode: string, content: JSONContent` | `{ docId: number, updatedAt: string }` |